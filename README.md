# poc-gitops-helm-templates

## USO
Instala helm en tu ambiente y clona este repositorio. Una vez clonado, entra al directorio de este repositorio en tu local y ejecuta lo siguiente:

```bash
helm template .